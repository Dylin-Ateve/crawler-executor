# Spec-Driven Development Workspace

This directory follows the Spec Kit style for organizing specification-driven work.

## Workflow

1. Update `.specify/memory/constitution.md` with project principles.
2. Refine `specs/001-scrapy-distributed-crawler/spec.md` until requirements are clear.
3. Fill `plan.md`, `research.md`, `data-model.md`, `contracts/`, and `quickstart.md`.
4. Generate `tasks.md` only after the plan and design artifacts are stable.

The original input document remains at:

`scrapy-distributed-crawler-feature.md`

