# Contracts

This directory will contain external contracts once requirements are clarified.

Expected contracts:

- Kafka message schema for `page-metadata`.
- Kafka message schema for `crawl-events`.
- Kafka message schema for `parse-tasks`.
- Dead-letter message schema for failed storage or publish operations.
- Optional operational API contracts for health and readiness endpoints.

